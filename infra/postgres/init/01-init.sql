-- Primary application database bootstrap.
-- Runs once on first container start (empty data volume).

CREATE SCHEMA IF NOT EXISTS pramaan;

-- No tables yet. Schema migrations will be owned by backend-java.
